# How To Install

To Install Needed Dependencies, please add the JUNG jars to the classpath executing the following command:
sudo cp utils/jung2-2_0_1/*.jar /usr/java/packages/lib/ext/

Without this command, antlr4-build won't work, even if Eclipse doesn't give any error.
